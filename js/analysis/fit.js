import * as THREE from'three';export function printerFit(root,p){root.updateMatrixWorld(true);const box=new THREE.Box3().setFromObject(root),s=box.getSize(new THREE.Vector3()),issues=[];if(s.z>p.z)issues.push('Z overflow');if(p.shape==='rectangular'){if(s.x>p.x)issues.push('X overflow');if(s.y>p.y)issues.push('Y overflow')}else{const r=p.diameter/2;root.traverse(o=>{const a=o.geometry?.attributes.position;if(!a)return;for(let i=0;i<a.count&&!issues.includes('outside circular bed');i++){const v=new THREE.Vector3().fromBufferAttribute(a,i).applyMatrix4(o.matrixWorld);if(Math.hypot(v.x,v.y)>r)issues.push('outside circular bed')}})}return{fits:!issues.length,issues}}
