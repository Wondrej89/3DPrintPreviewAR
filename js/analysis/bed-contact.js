import * as THREE from'three';export function bedContact(root,tolerance=.1){let area=0;const a=new THREE.Vector3(),b=new THREE.Vector3(),c=new THREE.Vector3();root.traverse(o=>{const p=o.geometry?.attributes.position;if(!p)return;for(let i=0;i<p.count;i+=3){a.fromBufferAttribute(p,i).applyMatrix4(o.matrixWorld);b.fromBufferAttribute(p,i+1).applyMatrix4(o.matrixWorld);c.fromBufferAttribute(p,i+2).applyMatrix4(o.matrixWorld);if(Math.max(a.z,b.z,c.z)<=tolerance)area+=new THREE.Triangle(a,b,c).getArea()}});return{contactArea:area}}
