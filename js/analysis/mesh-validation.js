export function validateInWorker(root){let count=0;root.traverse(o=>{if(o.geometry)count+=o.geometry.toNonIndexed().attributes.position.array.length});const all=new Float32Array(count);let at=0;root.traverse(o=>{if(!o.geometry)return;const a=o.geometry.toNonIndexed().attributes.position.array;all.set(a,at);at+=a.length});const worker=new Worker(new URL('../workers/mesh-analysis.worker.js',import.meta.url),{type:'module'});return new Promise((ok,no)=>{worker.onmessage=e=>{worker.terminate();ok(e.data)};worker.onerror=no;worker.postMessage(all.buffer,[all.buffer])})}
