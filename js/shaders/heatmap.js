import * as THREE from'three';export function heatmapMaterial(){return new THREE.ShaderMaterial({vertexShader:`attribute float risk;varying float vRisk;void main(){vRisk=risk;gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.);}`,fragmentShader:`varying float vRisk;void main(){vec3 safe=vec3(.12,.75,.55),warn=vec3(1.,.72,.05),bad=vec3(.95,.15,.12);gl_FragColor=vec4(vRisk<.5?mix(safe,warn,vRisk*2.):mix(warn,bad,(vRisk-.5)*2.),1.);}`})}
