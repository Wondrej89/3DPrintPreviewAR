import * as THREE from 'three';
export function heatmapMaterial(mode:'overhang'|'thickness'|'contact',nozzle=.4){return new THREE.ShaderMaterial({side:THREE.DoubleSide,uniforms:{uMode:{value:mode==='overhang'?1:mode==='thickness'?2:3},uNozzle:{value:nozzle}},vertexShader:`varying vec3 n;varying vec3 wp;void main(){n=normalize(normalMatrix*normal);wp=(modelMatrix*vec4(position,1.)).xyz;gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.);}`,fragmentShader:`precision highp float;varying vec3 n;varying vec3 wp;uniform float uMode;uniform float uNozzle;void main(){vec3 c;if(uMode<1.5){float risk=smoothstep(.25,.85,1.-dot(normalize(n),vec3(0.,0.,1.)));c=mix(vec3(.25,.85,.55),vec3(1.,.1,.04),risk);}else if(uMode<2.5){float proxy=abs(dot(normalize(n),vec3(0.,0.,1.)));c=mix(vec3(.94,.12,.18),vec3(.2,.85,.65),smoothstep(.15,.8,proxy));}else{float contact=1.-smoothstep(0.,max(.15,uNozzle),wp.z);c=mix(vec3(.22,.25,.32),vec3(.18,.9,.75),contact);}gl_FragColor=vec4(c,1.);}`})}
