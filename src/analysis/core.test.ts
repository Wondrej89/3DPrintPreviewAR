import {describe,expect,it} from 'vitest';import * as THREE from 'three';import {fitPrinter,geometryDimensions,overhangAngle,validateMesh,wallSeverity} from './core';import {toMillimeters} from '../three/units';import {trimLRU} from '../storage/db';
describe('geometry fundamentals',()=>{it('converts every supported unit to internal millimeters',()=>{expect(toMillimeters(2,'cm')).toBe(20);expect(toMillimeters(2,'inch')).toBe(50.8)});it('computes a scaled bounding box (STL/OBJ scaling)',()=>{const g=new THREE.BoxGeometry(1,2,3);expect(geometryDimensions([g],10)).toEqual({x:10,y:20,z:30})});it('checks rectangular and true circular footprints',()=>{const pts=[new THREE.Vector3(-5,-5,0),new THREE.Vector3(5,5,10)];expect(fitPrinter(pts,{shape:'rectangular',x:10,y:10,z:10}).fits).toBe(true);expect(fitPrinter(pts,{shape:'circular',diameter:10,z:10}).fits).toBe(false)});it('calculates overhang angle',()=>expect(overhangAngle(new THREE.Vector3(0,0,-1))).toBe(180));it('scales wall severity with nozzle',()=>{expect(wallSeverity(.3,.4)).toBe('critical');expect(wallSeverity(.3,.25)).toBe('ok')})});
describe('validation and storage',()=>{it('finds the three boundary edges of an open triangle',()=>expect(validateMesh([0,0,0,1,0,0,0,1,0]).boundaryEdges).toBe(3));it('keeps ten newest recent models',()=>{const data=Array.from({length:11},(_,lastOpened)=>({lastOpened}));expect(trimLRU(data)).toHaveLength(10);expect(trimLRU(data)[0].lastOpened).toBe(10)})});
